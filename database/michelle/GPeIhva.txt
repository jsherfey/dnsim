% Parameters:
ghva = [0.03]
Eca = [120]
 
% Functions:
GPeIhva(V,m) = -ghva.*m.*(V-Eca)
minf(V) = ((1.0) ./ (1 + exp((-20.0 - V)/7.0)))
taum = 0.2

% ODEs:
m' = (minf(V)-m)./taum

% Initial Conditions
m(0) = zeros(Npop,1)
 
% Substitution:
current =>  GPeIhva(V,m)
GPeIhva => -GPeIhva(V,m)

