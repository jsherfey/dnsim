gna = [100]
Ena = [50]
 
INa(V,h,m) = -gna.*h.*m.^3.*(V-Ena)
 
m' = 0.32*(V+54)./(1-exp(-(V+54)/4)).*(1-m)-0.28*(V+27)./(exp((V+27)/5)-1).*m;
m(0) = zeros(Npop,1)
h' = 0.128*exp(-(V+50)/18).*(1-h)-4./(1+exp(-(V+27)/5)).*h;
h(0) = zeros(Npop,1)
 
current => INa(V,h,m)
 
